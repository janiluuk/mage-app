# Codex Task Prompts

## Realtime hardening
Implement the smallest safe step toward event-driven admin updates. Keep polling as fallback. Add unit tests for duplicate and out-of-order events, integration tests for reconnect recovery, update docs, and capture screenshots for loading/live/error states.

## Shared presets
Move important presets to server-backed storage with local cache as optimization. Add migration path from localStorage/IndexedDB. Add tests for CRUD, conflict handling, and multi-client visibility. Capture screenshots.

## Capability routing
Introduce a capability-aware worker selection layer for AI/media jobs. Add tests for matching, rejection, and retry safety. Update architecture docs and add one diagnostic screenshot from admin.
